// createReservation(req, res)           // User reserves a room type
// getAllReservations(req, res)         // Admin view
// getUserReservations(req, res)        // User dashboard
// cancelReservation(req, res)
// assignRoomFromReservation(req, res)  // Admin assigns specific room